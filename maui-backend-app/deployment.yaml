apiVersion: apps/v1
kind: Deployment
metadata:
  name: maui-backend
  namespace: default
spec:
  replicas: 2
  selector:
    matchLabels:
      app: maui-backend
  template:
    metadata:
      labels:
        app: maui-backend
    spec:
      # imagePullSecrets:
      #   - name: ghcr-secret
      containers:
        - name: maui-backend
          image: ghcr.io/mikejohnpp/maui-backend:3286993a8283770d24ddad136828d719035eaae0
          ports:
            - containerPort: 8080
          # resources:
          #   limits:
          #     memory: "256Mi"
          #     cpu: "500m"
          #   requests:
          #     memory: "128Mi"
          #     cpu: "250m"
---
apiVersion: v1
kind: Service
metadata:
  name: maui-backend
  namespace: default
spec:
  selector:
    app: maui-backend
  ports:
    - port: 3000
      targetPort: 8080
  type: ClusterIP
