apiVersion: apps/v1
kind: Deployment
metadata:
  name: srl-service
  namespace: default
spec:
  replicas: 1
  selector:
    matchLabels:
      app: srl-service
  template:
    metadata:
      labels:
        app: srl-service
    spec:
      # imagePullSecrets:
      #   - name: ghcr-secret
      containers:
        - name: srl-service
          image: ghcr.io/mikejohnpp/srl-service:42a50ea93b856206459c13d1bd49ad029c5cd1bc
          ports:
            - containerPort: 8080
          env:
            - name: SPRING_PROFILES_ACTIVE
              value: "prod"
          # resources:
          #   limits:
          #     memory: "256Mi"
          #     cpu: "500m"
          #   requests:
          #     memory: "128Mi"
          #     cpu: "250m"
---
apiVersion: v1
kind: Service
metadata:
  name: srl-service
  namespace: default
spec:
  selector:
    app: srl-service
  ports:
    - port: 3000
      targetPort: 8080
  type: ClusterIP
